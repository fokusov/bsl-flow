# Оригинальный запрос

## Запрос пользователя (2026-09-15, планирование следующего релиза)

> 1. настройки модели ревьюеров нужно хранить в профиле пользователя, а не в проекте. Но если есть другие настройки (модель, апи ключ, и так далее) то берем из проекта

## Подтверждение пользователя

На вопрос о приоритетах пользователь подтвердил: «да, все так»:

- привязки «роль → модель», профили моделей и провайдеров совета — личная настройка рабочего места → профиль пользователя;
- если проект задаёт те же настройки сам — берём из проекта (проект важнее профиля);
- секреты остаются в текущих местах (`.bsl-flow/providers.local.yaml` / `token_env`).

## Контекст планирования

Пункт 1 плана следующего релиза (обсуждение в сессии ZCode 2026-09-15). Анализ на момент планирования: пользовательского уровня конфигурации в коде нет; `TrustedConfigRoot()` в cli/internal/platform/configpaths.go — неиспользуемая заготовка; весь совет настраивается в проектном `bsl-flow.yaml`.
