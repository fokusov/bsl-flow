# Исходный запрос

Источник обсуждения:

> [https://chatgpt.com/share/6aa3b681-648c-83ed-8212-99221c45cbe3](https://chatgpt.com/share/6aa3b681-648c-83ed-8212-99221c45cbe3) вот тут у меня диалог по построению архитектурного совета для слабых моделей. Давай вынесем это в отдельную спеку тоже, но вначале обсудим все слабые места и что могло остаться непродуманным

Уточнение границы runtime:

> Согласен. А qwencode пока вообще вынесем за рамки этой задачи, там чисто размышления по нему. Далее, по архитектурному агенту: я хочу отказаться от opencode так как он часто зависает и для моих коллег могут быть проблемы с его настройкой. Тут наверное нужен обычный вызов api llm, я бы хотел в конфиге задать модели для каждого такого агента/роли. Например, я пишу спеку с gpt sol, но ревьить ее хочу в deepseek flash. Брейнсторм может быть вообще отдан gpt astra. Короче, мне надо чтобы мою спеку обдумали разные LLM со всех сторон, выявили недостатки и сделали ее исполняемой. Вот как то так

Уточнение конфигурации ролей и API:

> да, `brainstorm` мы в конфиге можем сделать обязательным, а по умолчанию он выключен. То есть по умолчанию сделай как ты считаешь нужным, но пользователь может их включить как сам захочет. Также в конфиге должна быть возможность указания BASE_URL и токенов для каждого api. Они не обязательные, но если указаны, то используем выбранный api, если нет токена, то используем текущего агента

Выбранная семантика fallback:

> Та же модель, fresh context

Подтверждение оформления:

> да, все так. давай оформим это в спецификацию

