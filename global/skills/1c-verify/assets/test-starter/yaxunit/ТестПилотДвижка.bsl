// Минимальный независимый от конфигурации пилот YAxUnit.
// Его допустимо добавить в общий модуль тестового расширения и выполнить первым.

Процедура ИсполняемыеСценарии() Экспорт
	ЮТТесты.ДобавитьТест("ПоложительныйКонтрольДвижка");
	// Отрицательный контроль намеренно НЕ зарегистрирован: обычный прогон обязан остаться зелёным.
	// Для проверки отчёта об ошибке временно зарегистрируйте ОтрицательныйКонтрольОтчета
	// в отдельной локальной копии модуля, затем отмените это изменение.

КонецПроцедуры

Процедура ПоложительныйКонтрольДвижка() Экспорт

	ЮТест.ОжидаетЧто(1 + 1).Равно(2);

КонецПроцедуры

Процедура ОтрицательныйКонтрольОтчета() Экспорт

	ЮТест.ОжидаетЧто(1 + 1).Равно(3);

КонецПроцедуры
