// РЕЦЕПТ SafeMode fixture. Это исходник тестового расширения, а не чтение файла из 1С-сеанса.
// Локальные значения подставляются генератором/сборкой ДО компиляции и не попадают в поставку.
// В SafeMode модуль получает только Структуру примитивных значений и вызывает подтверждённый
// проектный конструктор; он не обращается к файловой системе, COM или внешнему соединению.

Функция ПолучитьЛокальнуюФикстуру() Экспорт

	// Заменить в локальной, игнорируемой копии перед компиляцией тестового расширения.
	// Возврат Новый Структура("Ключ", "<локальное значение>");
	ВызватьИсключение "Локальная fixture не подготовлена для этого проекта.";

КонецФункции
