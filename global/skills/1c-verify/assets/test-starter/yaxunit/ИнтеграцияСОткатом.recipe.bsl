// РЕЦЕПТ, НЕ ГОТОВЫЙ К ЗАПУСКУ: привяжите к реальной операции и чтению состояния проекта.
// Не подставляйте вымышленные справочники, документы или регистры.
// Внешние HTTP-вызовы, файлы, очереди и фоновые задания транзакция не откатывает;
// до прогона их надо отключить, заменить тестовым адаптером или проверить отдельно.

Процедура ИсполняемыеСценарии() Экспорт

	ЮТТесты.ДобавитьТест("<ИмяИнтеграционногоСценария>");

КонецПроцедуры

Процедура <ИмяИнтеграционногоСценария>() Экспорт

	// До транзакции: прочитайте исходное состояние через проверенный проектный API.
	// ПослеНачала = <ФактическоеЧтениеСостояния>();
	НачатьТранзакцию();
	Попытка
		// <ВызватьРеальнуюОперациюБезВнешнихПобочныхЭффектов>();
		// Внутри транзакции: утверждение наблюдаемого изменённого состояния.
		// ЮТест.ОжидаетЧто(<ФактическоеЧтениеСостояния()>).Равно(<ОжидаемоеЗначение>);
	Исключение
		ОтменитьТранзакцию();
		ВызватьИсключение;
	КонецПопытки;
	ОтменитьТранзакцию();

	// После отката отдельным чтением докажите возврат к исходному состоянию.
	// ЮТест.ОжидаетЧто(<ФактическоеЧтениеСостояния()>).Равно(ПослеНачала);

КонецПроцедуры
